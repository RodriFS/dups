# dups
A rather unperformant duplicate files finder

## How to run:
download de executable

run:
```
dups [Options] [Path]
```

Options:
  -r --recursive: Finds files in subdirectories

Path:
  The path of the folder you want to search the duplicates in
